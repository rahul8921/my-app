kind = "web"
previewPath = "/"
title = "Betting App"
version = "1.0.0"
id = "artifacts/betting-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24735

[services.development]
run = "pnpm --filter @workspace/betting-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/betting-app", "run", "build" ]
publicDir = "artifacts/betting-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24735"
BASE_PATH = "/"
